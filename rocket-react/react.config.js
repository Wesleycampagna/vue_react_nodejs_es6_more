/* const path = require('path')

module.exports = {
    outputDir: path.resolve(__dirname, '../rocket-node-js/src/public'),
    devServer: {
        proxy: {
            '/api': {
                target: 'htttp://localhost:8071/'
            }
        }
    }
} */